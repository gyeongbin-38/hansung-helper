# 구현 및 검증 기록 — 2026-09-14

## 구현
- 학교 학번/비밀번호 인증 후 계정 생성 또는 기존 계정 복귀.
- 기본 정보 → 수업 성향 6문항 → 홈. 설정 중간 저장, 설문 건너뛰기.
- D1에 계정별 프로필/선호/계획/강의 조회 결과 저장. 서버 세션으로 소유권 검사.
- 비밀번호와 학교 쿠키는 보관하지 않음. HTTPS 호스트 제한, HttpOnly 세션, Origin 검사, 로그인 제한.
- 요청한 색상/타이포그래피/버튼 토큰 반영. Toss Product Sans는 설치된 경우 사용하며 파일을 재배포하지 않음.
- 홈 장식 배너를 제거하고 실제 코스모스 강의·학사 현황·다음 행동으로 정리.

## 확인
- 사용자 승인 계정으로 학교 보호된 메뉴 접근, 코스모스 강의 7개 조회.
- school.test.mjs 5개 통과: 파싱, 쿠키 호스트 분리, HTTPS 전환, 실패, 외부 리다이렉트 차단.
- account-db.py 통과: 스키마 적용, 세션 소유권/만료, 계정 삭제와 다른 계정 분리.
- http-check.py 통과: 비로그인 읽기/수정/삭제 차단, Origin 검사, 입력 검증.
- 로컬 Wrangler가 간헐적으로 worker restarted mid-request 503을 반환함. HTTP 테스트는 이 특정 개발 환경 오류만 재시도. 공개 환경 검증은 미완료.
- tsc, 수정 애플리케이션 oxlint, production build 통과. 기존 미사용 starter components 전체 lint 문제는 이번 변경 범위 밖.

## 범위와 제한
- 종합정보시스템은 인증 상태 확인까지, 코스모스는 첫 화면 강의명/링크 스냅샷까지 검증.
- 성적/전체 이수학점/과제/출석/비교과 포인트/공식 SSO 제휴는 미구현 또는 미검증.
- 브라우저 시각·클릭 검증과 배포 환경의 로그인/DB 통합 검증은 미완료.
- 현재 Sites 커넥터 개인 계정에서 기존 project_id 조회가 project_not_found. 사이트 목록도 비어 있음.
- 기존 사이트 계정/워크스페이스 재연결 후 같은 project_id로 소스 push, 버전 저장, 공개 배포 및 로그인 검증 필요.
- 기존 공개 주소는 이전 버전 유지. 새 버전은 아직 공개되지 않음.

## 배포 주의
- drizzle/0000_academic_accounts.sql은 아직 운영에 적용되지 않음. 운영 적용 뒤 수정 금지.
- 배포 아카이브에 로컬 .wrangler 상태나 outputs 테스트 데이터 포함 금지.
- 사용자 자격증명과 개인 강의 내용은 저장소 및 검증 기록에 포함하지 않음.
